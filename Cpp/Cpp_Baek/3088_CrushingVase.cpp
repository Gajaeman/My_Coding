#include <iostream>
#include <vector>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N; cin >> N;
    vector<vector<int>> v(N, vector<int>(3));
    for(int i=0; i<N; i++){
        cin >> v[i][0] >> v[i][1] >> v[i][2];
    }

    vector<int> cal(N);
    for(int i=0; i<N; i++){
        while(true){   
        }
    }
}